# Copy to .env and fill in. .env is gitignored and must never be committed.
# Per README section 9: the MODEL NAME is NOT configured here on purpose --
# it is declared in source code (src/config.py) so graders can read it.
OPENAI_API_KEY=sk-...

# Optional: override the API base (Azure / proxy / local gateway).
# OPENAI_BASE_URL=https://api.openai.com/v1
